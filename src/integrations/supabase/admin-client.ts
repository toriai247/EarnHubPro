
// The Admin Client and Service Role Key have been removed for security reasons.
// All interactions should now be handled via the standard client with proper Row Level Security (RLS) policies.

export const supabaseAdmin = null;
